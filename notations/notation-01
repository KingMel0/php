classe = assuntos que vc cria para organizar sua classe.

atributo = variável que ganha mais recurso, armazenar.

encapsulamento = controlar o acesso.

método = função que ganha mais recurso, executar.

instância = quando vc utiliza a classe.

objeto = umma variável que representa ou tira uma cópia da classe.